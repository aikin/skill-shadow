<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thoughtworks.shadow.mybatis.mapper.AccountMapper">
    <select id="selectAccount" parameterType="int" resultType="com.thoughtworks.shadow.mybatis.model.Account">
        SELECT * FROM Account WHERE id = #{id}
    </select>
</mapper>
